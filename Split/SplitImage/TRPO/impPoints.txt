1. Images take in by the conv gaussain policy are flattened --- is this okay?
2. For the variable assign to work properly, the batch size needs to be constant. That means the last trajectory is cut-off, if total number of batch steps exceed the set batch size